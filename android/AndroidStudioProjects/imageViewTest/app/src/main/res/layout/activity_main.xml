<?xml version="1.0" encoding="utf-8"?>
<!--
     ImageView위젯
     - 아이콘 이미지와 같은 이미지들을 간단하게 표시 하는데 사용되는 위젯(뷰)
     - ImageView클래스는 TextView클래스를 확장 한것으로
       이미지를 표시할수 있는 TextView라고 생각 하면 된다.

     ImageView클래스의 속성 중에서..

     android:src속성
     -> 이미지를 표시하기 위해 이미지파일이 있는 경로를 설정하는 속성
     -> android:src="@참조할패키지경로/이미지파일이름"

    android:adjustViewBounds속성
    -> true로 설정 하면  이미지뷰의 가로 , 세로를 조정 할수 있도록 설정

    android:maxHeight속성
    -> 이미지 뷰의 최대 높이 (세로길이) 설정

   android:maxWidth속성
   -> 이미지 뷰의 최대 너비 (가로 길이) 설정정

-->

<ImageView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:adjustViewBounds="true"
    android:maxHeight="100dp"
    android:maxWidth="200dp"
    android:tint="#80917575"
    android:src="@drawable/androids">

</ImageView>











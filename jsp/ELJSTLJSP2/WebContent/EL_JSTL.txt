p510~

http://tomcat.apache.org/

taglibs 탭에 들어가서 

mpl:
taglibs-standard-impl-1.2.5.jar (pgp, sha512)
Spec:
taglibs-standard-spec-1.2.5.jar (pgp, sha512)
EL:
taglibs-standard-jstlel-1.2.5.jar (pgp, sha512)

세개 다운로드 후 프로젝트 WebContent/WEB-INF/lib에 추가


EL (Expression Language) : 표현언어 -> JSP 표현식을 다른형태로 사용

JSTL (JSP Standard Tag Library) : jsp에서 자주 사용하는 기능을 모아놓은 태그
	- 자카르타회사에서 제공하는 커스텀 태그 라이브러리

	- CORE 라이브러리 (기본기능)
	 -> 변수선언, 조건문, 반복문,...
	 -> 페이지 이동
	 
	- Formatting 라이브러리  
	 -> 국제화 : 다국어 처리
	 -> 형식화 : 날짜, 숫자 처리
	 
	- DB 라이브러리 
	 -> CRUD 기능
	 
	- XML 라이브러리
	 -> XML 문서처리
	 
	

